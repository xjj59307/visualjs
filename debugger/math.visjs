operator: pattern {
    exec plus(op = '+', parent = node) when (this.op === 0),
    exec minus(op = '-', parent = node) when (this.op === 1),
    exec times(op = '*', parent = node) when (this.op === 2),
    exec divide(op = '/', parent = node) when (this.op === 3)
}

plus: action {
    create node=tree_node(label = op),
    create tree_edge(from = parent, to = node),
    next this.left,
    next this.right
}
