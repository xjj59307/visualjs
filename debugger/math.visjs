operator: pattern {
    exec plus(label = '+') when (this.op === 0),
    exec minus(label = '-') when (this.op === 1),
    exec times(label = '*') when (this.op === 2),
    exec divide(label = '/') when (this.op === 3)
}
